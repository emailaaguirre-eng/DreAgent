# Core Dependencies (Required)
PyQt6>=6.4.0
python-dotenv>=1.0.0
openai>=1.0.0
requests>=2.28.0

# Speech Recognition (Optional but recommended)
SpeechRecognition>=3.10.0
pyaudio>=0.2.11

# Text-to-Speech (Optional - at least one recommended)
edge-tts>=6.1.0
gtts>=2.4.0
pyttsx3>=2.90

# Audio Playback (Optional but recommended for better experience)
pygame>=2.1.0
# Note: playsound may have build issues on some systems - pygame is sufficient for audio playback
# playsound>=1.3.0

# Emoji Support (Recommended for better emoji search)
emoji>=2.0.0
